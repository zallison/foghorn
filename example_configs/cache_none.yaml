# Example: disable caching
server:
  listen:
    udp:
      enabled: true
      host: 0.0.0.0
      port: 5353

  cache:
    module: none

  dnssec:
    mode: passthrough
    validation: upstream_ad
    udp_payload_size: 1232

upstreams:
  endpoints:
    - host: 1.1.1.1
      port: 53
  strategy: failover
  max_concurrent: 1

plugins: []

logging:
  level: info
  stderr: true
