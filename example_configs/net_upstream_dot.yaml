# Foghorn example: Upstream over DNS-over-TLS (DoT)
# Demonstrates multiple DoT upstreams with SNI and certificate verification.

listen:
  udp:
    enabled: true
    host: 127.0.0.1
    port: 5353
  tcp:
    enabled: false
  dot:
    enabled: false

upstreams:
  - host: 1.1.1.1
    port: 853
    transport: dot
    tls:
      server_name: cloudflare-dns.com
      verify: true
  - host: 9.9.9.9
    port: 853
    transport: dot
    tls:
      server_name: dns.quad9.net
      verify: true

# Per-upstream-attempt timeout (ms)
foghorn:
  timeout_ms: 2000
  upstream_strategy: failover
  upstream_max_concurrent: 1
  use_asyncio: true

dnssec:
  mode: passthrough
  validation: upstream_ad
  udp_payload_size: 1232

logging:
  level: info
