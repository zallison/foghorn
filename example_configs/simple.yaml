# Foghorn - DNS
listen:
  host: 0.0.0.0
  port: 5353
upstream:
  - host: 8.8.8.8
    port: 53
  - host: 1.1.1.1
    port: 53
plugins:
  - module: etc-hosts
logging:
  level: debug
# Optional: enable statistics
# statistics:
#   enabled: true
#   interval_seconds: 10
