"""Foghorn plugin namespace package.

Brief:
    Groups built-in Foghorn plugins under the ``foghorn.plugins`` namespace.

Inputs:
    - None.

Outputs:
    - Makes subpackages such as ``foghorn.plugins.cache``,
      ``foghorn.plugins.resolve``, and ``foghorn.plugins.querylog``
      importable for tests and runtime code.
"""
